function Title(css, textContent, smallContent) {

    console.log(css);

    return `
        <h1 style="${css[0]}${css[1]}">
            ${textContent}
        </h1>`

}

//limpar \n das posições do CSS

